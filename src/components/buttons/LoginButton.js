import React, { Component } from 'react';

const LoginButton = () => {
    return (
        <a href='/login' className='btn btn-default'>
            Login via Github
        </a>
    )
};

export default LoginButton;